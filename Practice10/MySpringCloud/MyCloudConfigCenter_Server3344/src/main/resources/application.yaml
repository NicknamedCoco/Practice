server:
  port: 3344
spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NicknamedCoco/Practice.git
          search-paths:
            - Practice
        default-label: main
        default-profile: dev
      label: main
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:7001/eureka
    eureka-server-connect-timeout-seconds: 10
    eureka-server-read-timeout-seconds: 10



