server:
  port: 80
spring:
  application:
    name: MyConsumerConsulOrder
  cloud:
    consul:
      port: 8500
      host: localhost
      discovery:
        service-name: ${spring.application.name}  #向注册中心注册的服务名称